import React from "react";

export default function PlayButton(props){ 
    return (
        <svg height="500" width="500">
            <polygon points="0,0 0,500 500,250" style={{fill:"black"}} />
            Sorry, your browser does not support inline SVG.
        </svg>
    )
}